extends template

block body
    p
      Welcome unknown user. Click one of these to continue:

    a(target='_top', href='#{conf.canvasURL}', scope='#{conf.canvasURL}')
      Full Page Canvas Login
    p
      div(class='fb-login-button')

